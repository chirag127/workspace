Run the deployment checklist and deploy. Steps: (1) confirm the target environment (staging or production), (2) run `pnpm build` and verify it passes, (3) run the linter — block on errors, (4) check that all required env vars are set, (5) run `wrangler deploy` or `gh workflow run` as appropriate for the repo. Report each step's status. If any step fails, stop and show the error — do not proceed to the next step.
