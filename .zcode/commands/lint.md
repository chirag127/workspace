Lint and auto-format all changed files in the current workspace. Run the project's configured linter (ESLint, Biome, or oxlint) and formatter (Prettier or Biome) on staged and unstaged changes. Fix auto-fixable issues silently. For issues that require manual intervention, list them with file:line references and the rule name. Report a summary: N files checked, M auto-fixed, K require manual fix.
