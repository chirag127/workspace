{
  "name": "oriz-flags-worker",
  "version": "0.1.0",
  "private": true,
  "description": "CF Worker serving runtime feature flags from D1+KV with nightly DR snapshot to gist.",
  "scripts": {
    "deploy": "wrangler deploy",
    "schema": "wrangler d1 execute oriz-flags --file=schema.sql",
    "tail": "wrangler tail",
    "test": "node --test src/__tests__/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^3.0.0",
    "@cloudflare/workers-types": "^4.20240924.0",
    "typescript": "^5.6.0"
  }
}
