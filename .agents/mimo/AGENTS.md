# Per-agent pointer — MiMoCode (mimo)

> **Read [`../../AGENTS.md`](../../AGENTS.md) first.** That file is the workspace source of truth.

MiMoCode auto-discovers `AGENTS.md` at workspace root (same as OpenCode), so this stub is informational only. It also reads `CLAUDE.md` as fallback. Place MiMoCode-specific overrides below this line if any are ever needed.

---

(none)
